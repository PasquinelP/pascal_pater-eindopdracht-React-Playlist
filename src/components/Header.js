import React from "react";
import Nav from "./Nav";

const Header = () => {
  return (
    <header className="page-header">
      <h1>React Playlist</h1>
      <Nav />
    </header>
  );
};

export default Header;
